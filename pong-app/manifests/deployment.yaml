apiVersion: apps/v1
kind: Deployment
metadata:
  name: pong-app-deployment
  namespace: exercises
spec:
  replicas: 1
  selector:
    matchLabels:
      app: pong-app
  template:
    metadata:
      labels:
        app: pong-app
    spec:
      containers:
        - name: pong-app-container
          image: pong-app:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 3000
